[package]
name = "accelerator"
version = "0.1.0"
edition = "2024"
description = "RICA accelerator — lightweight execution entry for Context Machine"

[dependencies]
machine = { path = "../machine" }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
reqwest = { version = "0.12", features = ["json"] }

[dev-dependencies]
tokio = { workspace = true }
