[package]
name = "machine"
version = "0.1.0"
edition = "2024"
description = "CM — Context Machine core primitives"

[dependencies]
utils = { path = "../utils" }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
rig = { workspace = true }
http = "0.2"
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt", "macros"] }
